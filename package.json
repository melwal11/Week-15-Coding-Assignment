{
  "devDependencies": {
    "json-server": "^0.17.3"
  },
  "scripts": {
    "start": "json-server --watch db.json --port 8000",
    "api": "json-server --watch db.json --port 8000 --routes routes.json"
  }
}
